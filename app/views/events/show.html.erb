<h1><%= @event.name %></h1>
<%= link_to "Edit", edit_event_path(@event) %> | <%= link_to "Delete", :event, method: :delete, data: { confirm: "Are you sure you want to delete #{@event.name}?" } %>

<p><%= @event.location %></p>
<p>Date: <%= @event.date.strftime('%A, %B %d, %Y') %><br>
	<!-- holds the time on the day event was created -->
Time: <%= @event.time.strftime('%I:%M %p') %></p>

<h3><%= @event.venue %></h3>
<p>address: <%= @event.venue_address %></p>

<h3>Invited Guests: </h3>
<p>Count: <%= @users.count %></p>
<!-- <ul>
<%# @invites.each do |i| %>
	<li><%#= i.user_id %></li>
<%# end %>
</ul> -->
<% @users.each do |one_invite| %>
<ul>
	<li><%= one_invite.email %></li>
</ul>
<% end %>

<%= link_to "Back to Events", events_path %>