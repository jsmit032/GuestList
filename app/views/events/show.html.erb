<h1><%= @event.name %></h1>
<%= link_to "Edit", edit_event_path(@event) %> | <%= link_to "Delete", :event, method: :delete, data: { confirm: "Are you sure you want to delete #{@event.name}?" } %>

<p><%= @event.location %></p>
<p>Date: <%= @event.date %><br>
Time: <%= @event.time %></p>

<h3><%= @event.venue %></h3>
<p>address: <%= @event.venue_address %></p>

<%= link_to "Back to Events", events_path %>